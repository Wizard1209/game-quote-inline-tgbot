[project]
name = "game-quote-inline-tgbot"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "Wizard1209", email = ""},
]
dependencies = [
    "aiogram>=3.4.1",
]
requires-python = "==3.12.*"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false
